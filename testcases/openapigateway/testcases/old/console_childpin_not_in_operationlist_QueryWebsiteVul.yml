- config:
    name: console_childpin_not_in_operationlist_QueryWebsiteVul.yml
    variables:
        - regionId: ${ENV(regionId)}
        - exist_child_pin_have_permission: ${ENV(exist_child_pin_have_permission)}
        - openapiGwUrl: ${ENV(openapiGwUrl)}
        - domain: ${ENV(domain)}
        - access_key_console: ${ENV(access_key_console)}
        - secret_key_console: ${ENV(secret_key_console)}

- test:
    name: 控制台aksk，传存在的子账号pin，调用不在白名单中的接口，调用成功
    request:
        url: /v2/websiteVuls/11
        method: GET
        headers:
            Content-Type: application/json
            x-jdcloud-pin: $exist_child_pin_have_permission
        params: ~
    setup_hooks:
        - ${update_JD_headers($request, $openapiGwUrl, None, csa, $access_key_console, $secret_key_console, $regionId)}
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
