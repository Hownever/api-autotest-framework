- config:
    name: back_childpin_have_no_permission_describeInstanceTypes.yml
    variables:
        - regionId: ${ENV(regionId)}
        - exist_child_pin_have_no_permission: ${ENV(exist_child_pin_have_no_permission)}
        - openapiGwUrl: ${ENV(openapiGwUrl)}
        - domain: ${ENV(domain)}
        - access_key_back: ${ENV(access_key_back)}
        - secret_key_back: ${ENV(secret_key_back)}

- test:
    name: 运营后台aksk，传存在的子账户pin，调用无权限的外部接口describeInstanceTypes，调用成功
    request:
        url: /v1/regions/$regionId/instanceTypes
        method: GET
        headers:
            Content-Type: application/json
            x-jdcloud-pin: $exist_child_pin_have_no_permission
        params: ~
    setup_hooks:
        - ${update_JD_headers($request, $openapiGwUrl, None, vm, $access_key_back, $secret_key_back, $regionId)}
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
