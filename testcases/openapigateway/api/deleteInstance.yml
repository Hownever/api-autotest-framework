name: 删除实例
description: 删除实例
operationId: deleteInstance

variables:
    product: vm
    api_ver: v1
    regionId: cn-north-1
    openapi_yaml: nofile
    AK: xxx
    SK: xxx
    header:
        Content-Type: application/json;charset=UTF-8
#        x-jdcloud-pin: $pin
    instanceId: $instanceId
    expected_status_code: 200


request:
    url: /$api_ver/regions/$regionId/instances/$instanceId
    method: DELETE
    headers: $header
setup_hooks:
    - ${hook_update_jdcloud_request($request, $AK, $SK, vm, $apiVersion, $openapi_yaml, $regionId)}
teardown_hooks:
    - ${hook_print("teardown_hooks for config")