name: 查询实例类型
description: 查询实例类型
operationId: describeInstanceTypes

variables:
    product: vm
    api_ver: v1
    regionId: cn-north-1
    openapi_yaml: nofile
    AK: xxx
    SK: xxx
    header:
        Content-Type: application/json;charset=UTF-8
#        x-jdcloud-pin: $pin

    expected_status_code: 200


request:
    url: /$api_ver/regions/$regionId/instanceTypes
    method: GET
    headers: $header
setup_hooks:
    - ${hook_update_jdcloud_request($request, $AK, $SK, vm, $apiVersion, $openapi_yaml, $regionId)}
teardown_hooks:
    - ${hook_print("teardown_hooks for config")