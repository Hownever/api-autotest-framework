- config:
    name: testUpdateUserApiGray.yml
    variables:
        tc_inner_data_gw_host: ${ENV(ENV_INNER_OPENAPI_DATA_HOST)}
        tc_outer_data_gw_host: ${ENV(ENV_OUTER_OPENAPI_DATA_HOST)}
        tc_api_ver: ${ENV(ENV_API_VERSION)}
        tc_header:
            Content-Type: application/json;charset=UTF-8
            x-jdcloud-pin: ${ENV(ENV_PIN)}
        tc_hufu_auth_ak: ${ENV(ENV_HUFU_AUTH_AK)}
        tc_hufu_auth_sk: ${ENV(ENV_HUFU_AUTH_SK)}
        tc_no_auth_ak: ${ENV(ENV_NO_AUTH_AK)}
        tc_no_auth_sk: ${ENV(ENV_NO_AUTH_SK)}
        tc_gray_pin: ${ENV(ENV_GRAY_PIN)}
        tc_gray_pin_user_ak: ${ENV(ENV_GRAY_PIN_USER_AK)}
        tc_gray_pin_user_sk: ${ENV(ENV_GRAY_PIN_USER_SK)}
        tc_no_gray_pin: ${ENV(ENV_NO_GRAY_PIN)}
        tc_no_gray_pin_user_ak: ${ENV(ENV_NO_GRAY_PIN_USER_AK)}
        tc_no_gray_pin_user_sk: ${ENV(ENV_NO_GRAY_PIN_USER_SK)}
        tc_openapi_yaml: ${ENV(ENV_OPENAPI_SWAGGER_FILE_PATH)}
        tc_regionId: ${ENV(ENV_DEFAULT_REGIONID)}
        tc_sleeptime: ${ENV(ENV_GW_CACHE)}
        tc_userinfo:
            not_exist_serviceName: not_exist_serviceName
            not_exist_apiName: not_exist_apiName
            not_exist_apiVersion: not_exist_apiVersion
            not_exist_result: 查询到的结果pins为空
#------------------------------------------------------------------------------------------------------------------------
            exist_serviceName: apigateway
            exist_apiName: describeApiGroups
            exist_apiVersion: v1
            exist_userPin: ${ENV(ENV_GRAY_PIN_BASE64)}
            exist_another_userPin: ${ENV(ENV_NO_GRAY_PIN_BASE64)}
            exist_result: 查询到的结果pins为exist_userPin
#------------------------------------------------------------------------------------------------------------------------
            not_exist_serviceName_result: not_exist_serviceName-v1-describeApiGroups不存在
            not_exist_apiVersion_result: apigateway-not_exist_apiVersion-describeApiGroups不存在
            not_exist_apiName_result: apigateway-v1-not_exist_apiName不存在
            new_pin: AAA
            many_pin: amRjbG91ZC1hcGktdGVzdA==,amRjbG91ZC1hcGlndy10ZXN0Mg==,DDD
    verify: False
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 测试前准备，删除业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}
    api: api/UserGray/deleteUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 创建业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}、pin为${get_value_from_userinfo($tc_userinfo, exist_userPin)}，创建成功
    api: api/UserGray/creatUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        userPin: ${get_value_from_userinfo($tc_userinfo, exist_userPin)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
    validate:
        - eq: [$scode, 200]
- test:
    name: 查询创建的业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，查询到的pin为：${get_value_from_userinfo($tc_userinfo, exist_userPin)}
    api: api/UserGray/describeUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
        - pins: content.result.pins
    validate:
        - eq: [$scode, 200]
        - eq:
          - $pins
          - ${get_value_from_userinfo($tc_userinfo, exist_userPin)}
#------------------------------------------------------------------------------------------------------------------------
#设置灰度时，使用两个pin请求，已设置的pin能通过，另一个网关报错
- test:
    name: 设置灰度后，在灰度名单中的pin：${get_value_from_userinfo($tc_userinfo, exist_userPin)}，调用接口describeApiGroups，能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_gray_pin_user_ak
        SK: $tc_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
- test:
    name: 设置灰度后，不在灰度名单中的pin：${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}，调用接口describeApiGroups，不能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_no_gray_pin_user_ak
        SK: $tc_no_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
        - error_message: content.error.message
        - error_status: content.error.status
    validate:
        - eq: [$scode, 403]
        - eq: [$error_status, HTTP_FORBIDDEN]
        - eq: [$error_message, "pin is not allowed in api gray [gw]"]
        - len_eq: [$requestId, 32]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 更新灰度pin：${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}，业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，更新成功
    api: api/UserGray/updateUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        userPin: ${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
    validate:
        - eq: [$scode, 200]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 使用非hufu的AKSK更新灰度pin，业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，报错没有权限
    api: api/UserGray/updateUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        userPin: ${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_no_auth_ak
        SK: $tc_no_auth_sk
    extract:
        - scode: status_code
        - message: content.error.message
        - status: content.error.status
    validate:
        - eq: [$scode, 403]
        - eq: [$message, 没有权限]
        - eq: [$status, OMPOPENAPI_HTTP_FORBIDDEN]
#------------------------------------------------------------------------------------------------------------------------
#更新灰度后，使用两个pin请求，已设置的pin能通过，另一个网关报错
- test:
    name: 更新灰度后，不在灰度名单中的pin：${get_value_from_userinfo($tc_userinfo, exist_userPin)}，调用接口describeApiGroups，不能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_gray_pin_user_ak
        SK: $tc_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
        - error_message: content.error.message
        - error_status: content.error.status
    validate:
        - eq: [$scode, 403]
        - eq: [$error_status, HTTP_FORBIDDEN]
        - eq: [$error_message, "pin is not allowed in api gray [gw]"]
        - len_eq: [$requestId, 32]

- test:
    name: 设置灰度后，在灰度名单中的pin：${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}，调用接口describeApiGroups，能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_no_gray_pin_user_ak
        SK: $tc_no_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 错误更新，不存在的业务线${get_value_from_userinfo($tc_userinfo, not_exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，结果为${get_value_from_userinfo($tc_userinfo, not_exist_serviceName_result)}
    api: api/UserGray/updateUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, not_exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        userPin: aaa
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
        - message: content.error.message
        - status: content.error.status
    validate:
        - eq: [$scode, 404]
        - eq: [$message, not_exist_serviceName-v1-describeApiGroups不存在]
        - eq: [$status, OMPOPENAPI_NOT_FOUND]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 错误更新，业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、不存在的版本${get_value_from_userinfo($tc_userinfo, not_exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，结果为${get_value_from_userinfo($tc_userinfo, not_exist_apiVersion_result)}
    api: api/UserGray/updateUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, not_exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        userPin: aaa
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
        - message: content.error.message
        - status: content.error.status
    validate:
        - eq: [$scode, 404]
        - eq: [$message, apigateway-not_exist_apiVersion-describeApiGroups不存在]
        - eq: [$status, OMPOPENAPI_NOT_FOUND]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 错误更新，业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、不存在的api名称${get_value_from_userinfo($tc_userinfo, not_exist_apiName)}，结果为${get_value_from_userinfo($tc_userinfo, not_exist_apiName_result)}
    api: api/UserGray/updateUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, not_exist_apiName)}
        userPin: aaa
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
        - message: content.error.message
        - status: content.error.status
    validate:
        - eq: [$scode, 404]
        - eq: [$message, apigateway-v1-not_exist_apiName不存在]
        - eq: [$status, OMPOPENAPI_NOT_FOUND]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 更新多个pin：${get_value_from_userinfo($tc_userinfo, many_pin)}，业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，更新成功
    api: api/UserGray/updateUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        userPin: ${get_value_from_userinfo($tc_userinfo, many_pin)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
    validate:
        - eq: [$scode, 200]
- test:
    name: 查询更新多个pin，业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，结果为${get_value_from_userinfo($tc_userinfo, many_pin)}
    api: api/UserGray/describeUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
        - pins: content.result.pins
    validate:
        - eq: [$scode, 200]
        - eq:
          - $pins
          - ${get_value_from_userinfo($tc_userinfo, many_pin)}
#使用两个pin请求，均能通过
- test:
    name: 多个pin设置灰度后，在灰度名单中的pin：${get_value_from_userinfo($tc_userinfo, exist_userPin)}，调用接口describeApiGroups，能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_gray_pin_user_ak
        SK: $tc_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
- test:
    name: 多个pin设置灰度后，在灰度名单中的pin：${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}，调用接口describeApiGroups，能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_no_gray_pin_user_ak
        SK: $tc_no_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
#------------------------------------------------------------------------------------------------------------------------
- test:
    name: 删除业务线${get_value_from_userinfo($tc_userinfo, exist_serviceName)}、版本${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}、api名称${get_value_from_userinfo($tc_userinfo, exist_apiName)}，删除成功
    api: api/UserGray/deleteUserApiGray.yml
    variables:
        #    url
        host: $tc_inner_data_gw_host
        regionId: $tc_regionId
        api_ver: $tc_api_ver
        serviceName: ${get_value_from_userinfo($tc_userinfo, exist_serviceName)}
        apiVersion: ${get_value_from_userinfo($tc_userinfo, exist_apiVersion)}
        apiName: ${get_value_from_userinfo($tc_userinfo, exist_apiName)}
        #    header
        header: $tc_header
        #    hooks
        AK: $tc_hufu_auth_ak
        SK: $tc_hufu_auth_sk
    extract:
        - scode: status_code
    validate:
        - eq: [$scode, 200]
#------------------------------------------------------------------------------------------------------------------------
#删除灰度后，使用两个pin请求，均能通过
- test:
    name: 删除灰度后，使用pin：${get_value_from_userinfo($tc_userinfo, exist_userPin)}，调用接口describeApiGroups，能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_gray_pin_user_ak
        SK: $tc_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]
- test:
    name: 删除灰度后，使用pin：${get_value_from_userinfo($tc_userinfo, exist_another_userPin)}，调用接口describeApiGroups，能通过
    api: api/UserGray/describeApiGroups.yml
    variables:
        host: $tc_outer_data_gw_host
        regionId: $tc_regionId
        AK: $tc_no_gray_pin_user_ak
        SK: $tc_no_gray_pin_user_sk
        filters1:
        filters1values:
        sleeptime: $tc_sleeptime
    extract:
        - scode: status_code
        - requestId: content.requestId
    validate:
        - eq: [$scode, 200]
        - len_eq: [$requestId, 32]