testcases:
    获取jdsf 服务所有的可用区信息:
        testcase: testcases\jdsf\apis\Region-describeRegions-200.yaml
        parameters:
            userpin:
                - ${ENV(ENV_USER_PIN)}
            usererps:
                - ${ENV(ENV_USER_ERP)}
            productline:
                - REGISTRY
                - OPEN_TRACE
