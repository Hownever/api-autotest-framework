testcases:
    正常删除，返回200和success:
        testcase: testcases\jdsf\apis\deleteTraceCluster-200.yaml
        parameters:
            userpin:
                - ${ENV(ENV_USER_PIN)}
            usererps:
                - ${ENV(ENV_USER_ERP)}
            instanceid:
                - reg-notexist
