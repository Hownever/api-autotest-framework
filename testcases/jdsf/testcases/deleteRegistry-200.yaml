testcases:
    删除不属于当前用户pin的注册中心，返回402:
        testcase: testcases\jdsf\apis\deleteRegistry-200.yaml
        parameters:
            userpin:
                - ${ENV(ENV_USER_PIN)}
            usererps:
                - ${ENV(ENV_USER_ERP)}
            registryid:
                - reg-exist