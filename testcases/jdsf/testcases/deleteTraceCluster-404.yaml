testcases:
    调用链id不存在，调用删除接口，返回404:
        testcase: testcases\jdsf\apis\deleteTraceCluster-404.yaml
        parameters:
            userpin:
                - ${ENV(ENV_USER_PIN)}
            usererps:
                - ${ENV(ENV_USER_ERP)}
            instanceid:
                - instance_id_not_exist
